### Требования к логгеру

разбиение логов на этапы

- Обработка пользовательского действие
- Вызов внутреннего api
- Обрабокта api запроса
- Отрисовка
-- Какие есть этапы отрисовки?
- Этары работы с внешним api

/**
 * Глобальное состояние логгера
 * Локальное состояния сценария логирования
 * Логирующие сценарии
 * - Точка старта
 * - Точка конца
 * - Лог
 * -- До вызова
 * -- После вызова
 * -- Средство логированя
 * Производные логирующие сценарии
 * - Один/Множество
 * - ??
 * - Трасеровка присвоения значения свойству
 * 
 * Определить прокси над вызовом метода 
 *  notify.beforeCall
 *  notify.afterCall
 * 
 * Logger.on('stateName', )
 * Фиксация созданных объектов внутри метода
 * - Включая дочерние
 * - Исключая дочерние
 */